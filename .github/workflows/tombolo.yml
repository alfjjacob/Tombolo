name: Build  
on: 
  push:
      branches:
        - master
jobs:
  build:
    runs-on: self-hosted
    steps:            
    - uses: actions/checkout@v2
    - run: |
        #build 
        docker build -t tombolo-ui ./client-reactjs
        #tag
        docker tag tombolo-ui tombolodevcrWjMH9.azurecr.io/tombolo-ui
        #login to container registry
        az login --identity
        az group list
        az acr login --name tombolodevcrWjMH9.azurecr.io
        #push image to acr
        #docker push tombolodevcrWjMH9.azurecr.io/tombolo-ui
        az acr repository list --name tombolodevcrWjMH9 --output table
    - uses: azure/webapps-deploy@v2
      with:
        app-name: 'tomboloui-eastus2-dev'
        #publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        images: 'tombolodevcrWjMH9.azurecr.io/tombolo-ui'
